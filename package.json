{
  "name": "intentswap",
  "version": "0.1.0",
  "private": true,
  "description": "Plug-and-play AI website personalization widget",
  "scripts": {
    "build": "node esbuild.config.js",
    "watch": "node esbuild.config.js --watch",
    "dev": "npx serve . --listen 3000"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "esbuild": "^0.24.0",
    "serve": "^14.2.0"
  }
}
